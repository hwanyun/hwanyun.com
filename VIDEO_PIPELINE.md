# 영상 파이프라인

사이트에 올라가는 모든 영상의 기준과 절차. 2026-08-30 무제 화질 사건에서
확정했다 — 같은 실수를 반복하지 않기 위한 문서다.

## 원칙 다섯

1. **마스터에서 한 번만 압축한다.** 중간 파일을 재인코딩하면 압축이
   겹친다. 무제 스틸이 삼중 압축으로 배포된 적이 있다.
2. **프레임레이트는 원본 그대로.** 무제(60fps)를 30fps로 낮췄더니
   파티클이 "렉 걸린 것처럼" 끊겨 보였다. 프레임레이트가 아까우면
   해상도를 낮춰 상쇄한다.
3. **faststart 필수.** AVAssetWriter는 moov 아톰을 파일 끝에 쓴다.
   그대로 올리면 브라우저가 파일 전체를 받기 전엔 재생을 시작하지
   못한다 — 87MB 파일에서 "재생이 안 된다"로 나타났다.
4. **폰에는 경량본.** 폰 표시 폭은 400px 남짓인데 데스크톱 해상도를
   그대로 받으면 대역폭에서 버벅인다. media 항목의 `srcMobile`,
   사운드 항목의 `backdropMobile`이 ≤700px에서 자동 선택된다.
5. **비트레이트는 실측으로 정한다.** 가장 조밀한 구간을 잘라
   2,500/4,000/6,000으로 뽑아 확대 비교한다. 콘텐츠마다 무릎점이
   다르다 — 어두운 방 + 빛나는 형상(비정형)은 1,700kbps로 충분하고,
   수백 스프라이트 + 잔글씨(무제)는 6,000kbps가 필요하다.
   **kbps 숫자만 보고 화질을 판단하지 말 것.**

## 여백은 인코딩에서 잘라낸다

무제 바닥은 원형 만다라가 프레임의 80%였다. CSS로 확대하면 흐려지지만,
인코딩에서 중앙만 취하면(txcrop) 같은 비트레이트가 실제 그림에만 쓰여
커지는 동시에 선명해진다.

## 도구 (전부 로컬 빌드, /tmp에 있음 — 없으면 세션 기록에서 재생성)

| 도구 | 역할 |
|---|---|
| `/tmp/tx` | 리사이즈+fps+비트레이트 트랜스코더 (AVFoundation) |
| `/tmp/txcrop` | 위와 같되 중앙 크롭 지원 (예: 0.82 = 82%만 취함) |
| `/tmp/faststart.py` | moov 아톰을 앞으로 (업로드 전 필수) |
| `/tmp/grabq` | 프레임 정확 스틸 추출 (JPEG 품질 지정, 0.95 사용) |
| `/tmp/mp4probe.py` | 길이·프레임수·fps 검증 |
| `/tmp/atoms.py` | 아톰 순서 확인 ([ftyp][moov]…이어야 함) |

절차: 마스터 → (tx|txcrop) → faststart → mp4probe·atoms 검증 →
`wrangler r2 object put` → **Cloudflare 캐시 퍼지** (같은 URL에
덮어쓰면 엣지가 옛 파일을 계속 준다) → 라이브에서 Content-Range로
파일 크기 확인.

## 마스터 위치

`~/Desktop/무제_영상_원본/` — 바닥 1280×1280@60fps 226Mbps(4.7GB),
벽면 1280×264@60fps 70Mbps(1.5GB). 건드리지 않는다. 공모전·상영 제출용.

## 현재 배포 사양 (2026-08-30)

| 파일 | 사양 | 용도 |
|---|---|---|
| mujae-floor-sync.mp4 | 1000×1000 @60fps 6,000k 크롭0.82, 130MB | 데스크톱 |
| mujae-wall-sync.mp4 | 1280×264 @60fps 5,000k, 105MB | 데스크톱 |
| mujae-floor-sync-sm.mp4 | 600×600 @60fps 2,200k 크롭0.82, 48MB | 폰 |
| mujae-wall-sync-sm.mp4 | 768×158 @60fps 1,800k, 38MB | 폰 |
| loops/mujae.mp4 | 800×800 @60fps 4초 크롭, 3.5MB | 홈 그리드 |
| 국면 스틸 12장 | 크롭본에서 grabq q0.95 | 작품 페이지 |

## 전시 기록 영상(mujae-flyer / mujae-sign)에 관하여

작품 페이지 상단의 사이클 영상 두 편은 **인코딩 문제가 없다** —
1920×1080 @30fps ~10,000kbps로, 비정형의 회로(960p, 1,700~3,000kbps)보다
사양이 오히려 높다. 화질이 낮아 보인다면 그것은 **촬영 자체의 한계**다:
어두운 전시장에서 프로젝션을 카메라로 찍은 기록이라 노이즈·모아레·
핸드헬드 흔들림이 소스에 들어 있고, 재인코딩으로는 개선되지 않는다.

끌어올리는 길은 둘뿐이다.
1. **재촬영** — 삼각대 + 어두운 곳에 강한 카메라로 전시장에서 다시 찍기
   (전시가 끝나 현실적으로 불가).
2. **직접 렌더로 대체·보강** — 시스템에서 뽑은 60fps 렌더(위 표)는 이미
   페이지에 있다. 기록 영상은 '공간 안의 작품'을 보여 주는 유일한
   자료이므로, 화질이 아니라 역할로 남긴다.
