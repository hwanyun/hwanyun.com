# HWANYUN 사이트 리디자인 로드맵

> 작성: 2026-08-15 · 기준 커밋: `1bb0867` (origin/main)
> 목표: 5년 뒤에도 유효한 시스템 위에, 작품 문법을 인용한 인터랙션을 얹어
> 최고 수준의 작가 포트폴리오와 견주어도 손색없는 사이트를 만든다.

## 방향 (요약)

- 포지셔닝: **"방문자의 존재가 입력 신호가 되는 아카이브."**
  작품 12점의 공통 문법 = 보이지 않는 신호를 감각 가능하게 바꾸는 변환기(transducer).
  사이트의 모든 인터랙션은 이 문법의 인용이어야 한다.
- 원칙 1 — **장식적 인터랙션 금지.** 새 효과마다 "어느 작품의 문법인가"에 답할 것.
  답이 없으면 넣지 않는다. (스크롤 하이재킹, 인트로, 커서 교체, 배경 자동재생 오디오 금지)
- 원칙 2 — **모든 인터랙션은 무시 가능.** 큐레이터는 30초 안에 CV와 대표작을 찾는다.
  `prefers-reduced-motion`이면 전부 꺼지고 깨끗한 아카이브만 남는다.
- 원칙 3 — 콘텐츠는 MD + 스키마가 원본. 표시 레이어는 언제든 갈아끼울 수 있게 유지.

---

## 완료된 작업 (이 브랜치, 미커밋)

- [x] **홈 티커 방향 반전** (우→좌) + 구체 표면 체감 속도 동기화
  - `src/pages/index.astro` — 속도는 `SPEED_FACTOR`(현재 1)로 조절. 낮추면 느려짐 (0.4 권장 검토)
  - 진행 중 전시는 `NOW ON` 태그, 예정은 `UPCOMING`
- [x] **/events 작가 관행 구조** — 진행 중+예정 단일 리스트(진행 중은 `Until …` 표기 + 점 마커),
  지난 일정은 연도별 역순 그룹. 분류는 브라우저에서 오늘 날짜 기준 자동 (재배포 불필요)
  - `src/pages/events/index.astro`

## 보류

- [ ] ~~퍼시스턴트 사운드 플레이어~~ — **보류** (사용자 결정, 2026-08-15)

---

## Phase 1 — 기초 정리 (인터랙션의 선행 조건)

시스템이 흔들리면 그 위의 인터랙션은 유지비만 늘린다. 먼저 한다.

- [x] **1-1. 디자인 토큰 통합** — `src/layouts/Base.astro` `:root`에 정의, 11개 파일 244곳 치환
  - 텍스트 4단계 `--text` / `--text-2` / `--text-3` / `--text-4`
  - 면·선 `--surface` / `--surface-2` / `--line` / `--line-2`
  - 어두운 면(히어로) `--void` / `--void-text` / `--void-text-2` / `--void-label` / `--void-line`
  - 타입 스케일 7단계 `--fs-display` … `--fs-2xs`
  - 의도적으로 남긴 팔레트: `sound.astro` 카테고리 색 7종, `contact.astro` 웜톤 블롭(→ 5-3에서 처리)
- [x] **1-2. 메뉴 단일화** — 3벌 → `src/components/SiteMenu.astro` 하나
  - variant 3종: `page`(일반 페이지) / `hero`(홈 히어로, 데스크톱 숨김) / `grid`(홈 그리드 오버레이, 데스크톱 전용)
  - 스타일은 Base.astro 전역 한 곳, 동작 스크립트도 하나(`.site-menu` 전부에 위임)
  - 검색 차이는 variant로 흡수: `grid`는 `#search` 실시간 필터(WorkGrid 스크립트가 읽음),
    나머지는 `/?q=` 폼 전송. Works 서브메뉴는 id 충돌·중복 렌더 방지를 위해 `grid`에서 제외
  - ~~Search 입력 스타일 통일~~ → 확인 결과 이미 동일(박스형). 불필요
- [x] **1-3. 푸터 연도 자동화** — `2014–{현재연도}`, 브라우저에서 보정하므로 재배포 없이도 정확
- [ ] **1-4. 아카이브 내구성** (디자인과 독립, 병행 가능)
  - [x] ~~SoundCloud 의존 제거~~ — **이미 완료되어 있었음**. 22/22곡이 자체 R2 호스팅이고
    `sounds.url`은 기록 보존용으로만 남아 페이지에서 사용하지 않는다. YouTube/Vimeo 임베드도 0건.
    (앞선 분석에서 낡은 체크아웃의 스키마를 근거로 잘못 지적했던 항목)
  - [x] **`assets.zyrosite.com` 이전 완료** — 저장소 전체 외부 이미지 참조 0건
    - `intuition-ii/` — thumbnail, 01 (원본 `DSC02280.jpg`에서 생성), sponsors/listhus.webp
    - `the-voice-of-stripes/` — 02는 원본 `Live E_edited.jpg`에서 크롭, 03은 원본 무크롭 복사
    - `the-voice-of-stripes/` thumbnail·01은 **라이브 CDN본을 내려받아 사용**.
      원본 `_MG_6580-1.jpg`는 EXIF상 세로(upper-right)인데 웹빌더에서 상단 와이어 영역만
      16:9로 크롭한 별도 파일이 올라가 있어, 원본에서 기계적으로 재현할 수 없었다.
      더 높은 해상도가 필요하면 5184px 원본에서 같은 프레이밍으로 다시 크롭할 것.
    - 교훈: sips가 보고하는 픽셀 크기는 EXIF 방향을 반영하지 않는다. 이미지 이전 시
      `file`로 orientation을 먼저 확인하고, 회전을 픽셀에 반영한 뒤 EXIF를 제거해야
      브라우저가 이중으로 회전시키지 않는다.
  - [ ] Google Fonts → 셀프호스팅 검토 (외부 요청 0으로)

## Phase 2 — 무음 루프 썸네일 (체류 시간 최대 레버)

작품은 전부 시간 기반인데 썸네일이 정지 이미지 = 작품의 90%를 버리는 표현.
Zimoun 홈의 체류 시간이 여기서 나온다.

- [x] **2-1. `thumbVideo` 필드 추가** — `src/content/config.ts` + `public/admin/config.yml`
- [x] **2-2. WorkGrid 렌더링** — `thumb` 이미지 위에 `<video muted loop playsinline preload="none">`를
  겹치고, 재생이 시작되면 페이드인. 로드 전·실패·reduced-motion에서는 thumb이 그대로 보인다
- [x] **2-3. 루프 클립 3종** (`public/videos/loops/`, 각 4초, 합계 2.3MB)
  - atypical-circuit ← `atypical-circuit-2.mp4` 4.5s~ (형태가 터져 나오는 구간)
  - coexistence ← `coexistence-1.mp4` 9s~ (투사면이 크게 잡힌 구간)
  - conductive-circles ← `conductive-circles-intro.mp4` 18s~ (원형 그리드 = 작품 시그니처)
  - the-voice-of-stripes는 **의도적으로 제외** — 보유 영상이 오실로스코프 화면이라
    설치 사진 썸네일이 더 강하다. 나중에 설치 기록 영상이 생기면 추가할 것
- [x] 성능·접근성 규칙 — `IntersectionObserver`로 뷰포트 밖 정지, 동시 재생 상한
  (데스크톱 6 / 모바일 2), 탭 백그라운드 시 전체 정지, `prefers-reduced-motion`·
  `saveData`·2G에서는 아예 로드하지 않음
- 도구 메모: 이 기기에 `ffmpeg`이 없다. macOS 내장 `avconvert`로 만들었다.
  `avconvert --source X --output Y --preset Preset640x480 --start 4.5 --duration 4 --replace`
  (오디오 트랙 제거 옵션은 없음. 비트레이트 직접 제어도 불가 — 프리셋과 길이로만 조절)

## Phase 3 — 커서 = 센서 (근접 반응 그리드) ✅

초음파 센서가 거리를 연속값으로 읽듯 — Atypical Circuit / Voice of Stripes 인용.

> 계획 당시 "호버 글리치를 거리 함수로 바꾼다"고 적었으나, **글리치는 현재 코드에 없다**
> (낡은 체크아웃에서 본 것). 실제 데스크톱 호버는 타일 확대 + 캡션 페이드인이었다.
> 그리고 **모바일에는 이미 연속 반응이 있었다** — 화면 중앙과의 거리로 밝기·확대·
> 스크림·캡션을 조절한다. 즉 이 문법은 이미 코드베이스에 있었고, 데스크톱만 입력을
> 스크롤 대신 커서 거리로 바꾸면 되는 일이었다.

- [x] **3-1.** 커서 ↔ 각 타일 사각형까지의 **최단 거리**(내부는 0)를 280px로 정규화해
  `--p`(0~1)를 타일에 주입. rAF로 프레임당 한 번만 계산
- [x] **3-2.** 표현은 CSS가 담당 — 썸네일 밝기 `0.93 + p*0.13`, 캡션 불투명도 `p*0.85`.
  호버 시에는 캡션 1.0으로 확정(정보 접근이 장식보다 우선)
- [x] 레이아웃은 건드리지 않는다 — 기존 hover-expand(트리맵 재배치)와 충돌 방지
- [x] 재측정 훅: 리사이즈·스크롤·transitionend·검색 필터 시 좌표 갱신,
  transitionend는 타일·속성마다 발생하므로 rAF로 묶음
- [x] `pointer: fine`이 아니거나 `prefers-reduced-motion`이면 아예 동작하지 않고,
  캡션은 호버 시에만 나타나는 기존 동작으로 폴백

## Phase 4 — 재료 회로 내비게이션 ✅

재료가 작품들을 잇는 실제 그래프다. 어떤 메이저 작가 사이트에도 없는 기능이면서,
재료 중심 사고의 구조화라 정당하다. 작품 페이지의 막다른 골목도 이것이 푼다.

- [x] **4-1. 통제 어휘 11종** — `src/data/materials.ts` 한 곳에서 고정(스키마·CMS·표시가 함께 따라간다)
  - copper 4 / sensor 4 / plastic-sheet 4 / speaker 3 / conductive-paint 2 /
    projection 2 / 3d-pen 2 / field-recording 2 / moving-image 2 / light 2 / hanji 1
  - **원칙**: 2개 이상 작품에 나타나는 재료만 태그로 삼는다(연결을 못 만드는 태그는 죽은 노드).
    `hanji`는 예외 — 1작품뿐이나 작업에서의 비중이 커서 작가 판단으로 포함
  - **`sound`는 의도적 제외** — 12작품 중 7개라 거의 전부를 연결해 내비게이션 기능을 못 한다.
    사운드는 재료가 아니라 실천의 층위
  - `polycarbonate`/`polyvinyl`/`plastic film` → `plastic-sheet`로 통합(빛과 소리를 통과시키는
    반투명 막이라는 같은 역할). 이 통합으로 4작품이 이어졌다
- [x] **4-2. 12개 작품 태깅 완료** — 고립 작품 없음
- [x] **4-3. 작품 상세** — `medium` 아래에 클릭 가능한 재료 태그
- [x] **4-4. 같은 재료의 작품** — 공유 재료가 많은 순, 같으면 최근작 순.
      예) Intuition I → 6작품으로 연결. 막다른 골목 해소
- [x] **4-5. `/works?material=` 필터 뷰** — 정적 사이트라 쿼리를 브라우저에서 해석.
      필터가 걸리면 남은 작품을 세로 트레이스 선과 접점으로 이어 회로처럼 보여 준다.
      `history.pushState`로 리로드 없이 상태 전환, 뒤로 가기 동작

### 남겨 둔 판단
- `3d-pen` 쌍(Intuition II ↔ Three Pieces II)은 주 그래프와 끊겨 있다. 작가 판단으로 그대로 둔다.
  Intuition II는 `series` 필드로 Intuition I과 이어져 있어 실제 막다른 골목은 아니다.

## Phase 5 — 그리드 ↔ 필드 토글 + 방문의 흔적

- [x] ~~**5-1. Works 인덱스 뷰 토글** — List ↔ Field~~ → **7-5에서 삭제**(2026-08-15)
  - 필드 뷰에서 작품은 입자 군집이 되고 연도 축으로 재배열된다. 재료가 많을수록 군집이 촘촘하다
  - 목록 DOM을 원본으로 삼아 캔버스에 다시 그리므로 검색·재료 필터·접근성 로직은 목록 쪽 하나만 유지된다
  - 커서가 가까우면 군집이 조여든다 — Phase 3의 근접 반응과 같은 문법
  - `prefers-reduced-motion`이면 애니메이션 없이 한 번만 그린다
- [x] **5-2. 방문의 흔적** — 시아노타입(감광지) 인용. `src/components/SeenTracker.astro`
  - 작품 페이지 방문 시 slug를 `sessionStorage`에 기록(세션 한정, 서버 전송 없음)
  - `/works` 필터 뷰: 방문한 작품은 **회로 접점이 채워진다** (Phase 4의 트레이스와 연결)
  - `/works` 기본 목록: 연도 앞 작은 점 하나. 필터 중에는 접점이 그 역할을 하므로 숨겨 이중 표시 방지
  - 홈 그리드: 캡션 제목 아래 노출 자국
  - 저장이 막힌 환경(프라이빗 모드 등)에서도 사이트는 그대로 동작한다
- [x] **5-3. Contact 톤 통일** — 마스코트는 유지, 배경만 중립화
  - 계획 시 "크림색 블롭을 입자 언어로 교체"라고 적었으나 **문제를 잘못 짚었다**.
    그 블롭은 배경 그라디언트였고, 그 위에 3D 마스코트(GLB, 애니메이션 11클립,
    클릭→점프·박수 / 호버→가리키기 / 멀어지면 뛰어오기)가 있다. 로컬 dev에서
    three.js 모델이 로드되지 않아 배경만 보였던 것 (라이브는 정상)
  - 작가 판단으로 **마스코트는 유지**. 대신 웜톤 두 곳을 중립화했다:
    배경 라디얼 그라디언트(크림 → `--surface-2`/`--surface`), 링크 활성 하이라이트
    (노랑 `#fff59d55` → `--surface-2`)
  - 커서 추적 인터랙션은 그대로 두고 색만 뺐다. 이제 이 화면에서 색을 갖는 것은
    마스코트뿐이고, 그 대비가 사고가 아니라 의도로 읽힌다
  - contact.astro의 하드코딩 hex 0개
  - **서식지 추가 (작가 요청)** — "구체를 복제하는 게 아니라 같은 결로 캐릭터가 사는 공간을".
    히어로가 허공의 구(球)라면 여기는 발 딛고 사는 장소이므로, 원근이 있는 입자 지면 +
    지평선 + 공중의 먼지로 만들었다. three.js와 무관한 2D 캔버스라 마스코트가
    로드되지 않아도 공간은 남는다
  - **공간이 캐릭터에 반응한다 (작가 요청)** — 마스코트 렌더 루프가 화면 좌표와 상태를
    `window.__mascot`으로 공개하고, 서식지가 읽는다
    · `mode === "busy"`(타자 치는 중) → 손끝에서 신호가 튀고 가까운 신호끼리 선으로 이어진다(전도)
    · 점프·박수 등 1회 동작 → `mascot:burst` 이벤트로 파문이 퍼진다
    · 레이어: 지면·공기는 캐릭터 뒤(z1), 캐릭터가 만드는 신호는 앞(z3)
  - 이메일 크기 상향 + 목적 문구 + 복사 버튼(클립보드 API 실패 시 execCommand 폴백,
    둘 다 막히면 버튼을 숨기고 링크만 남긴다)

---

## Phase 6 — "하나의 포트폴리오" (디자인 리뷰 후속)

- [x] **6-1. 작품 페이지 미디어 재조판** — 도록의 조판으로
  - VIDEO/IMAGES 타입 그룹·라벨 제거 — MD `media` 배열 순서가 곧 서사
  - 행 패턴 3종만: 전체폭 / 2장 나란히 / 단독 부분폭(align)
  - 2장 행은 실제 비율로 열 폭을 배분(JS)해 두 장의 높이·캡션 기선이 정확히 맞는다. 크롭 없음
  - 세로 영상(9:16)은 가로와 짝짓지 않는다 — 세로끼리 짝 또는 단독 중앙(340px)
  - 리빌 완화: blur(7px)+18px 이동 0.72s → opacity 0.45+6px 0.3s. 옵저버 문턱도 낮춤
  - 캐러셀(가로 트랙)·justified 절대배치·드래그 스크립트 제거 — 세로 편집 흐름으로 통일
  - 스폰서 로고 grayscale, 호버 시 원색 (크롬 무채색 원칙)
  - fedora-exile(비몰입 lead 경로)은 유지
- [x] **6-2. 폰트 — Pretendard Variable 셀프호스트**
  - `public/fonts/pretendard/` 동적 서브셋 92개(3.1MB 전체, 실제 로드는 필요한 블록만 — 확인 결과 9개)
  - Google Fonts 링크 2건 제거 → **외부 폰트 요청 0**
  - 라틴이 Inter 기반이라 한/영이 한 패밀리에서 정합. Noto의 넓고 밋밋한 라틴 문제 해소
  - 본문 웨이트 300 → **400**. 300은 18px 이상(h1)에서만 성립하며, 13px 메타·11px 라벨이
    300으로 내려가 저해상도에서 뭉개지던 문제를 고친다
  - 자간: 본문 −0.01em, h1 −0.02em
  - **히어로 캔버스 워드마크가 Arial이었다** — 사이트에서 가장 큰 타이포그래피가 시스템 폰트.
    Pretendard로 교체하고 `document.fonts.ready` 후 재샘플링(늦게 도착하면 폴백으로 찍히므로)
- [x] **6-3. 홈 그리드 — 거터 + 색 큐레이션 (A안)**
  - 트리맵 패킹은 이 사이트의 개성이므로 유지하고, 질서만 부여했다
  - 거터: 타일 `border` 2px → **4px** (인접 타일이 만나 실제 간격 8px)
  - 여백: 우 56 / 상하 14로 제각각이던 것을 **우·상·하 24px 균일**로.
    좌측은 메뉴 컬럼(210px) 유지
  - **색 큐레이션**: 12장 중 채도가 높은 3장(coexistence·conductive-circles·weve-opened)이
    소음의 주범이고 나머지 9장은 무채색 계열이다. 이전엔 채도 2장이 order 2·3으로 붙어
    충돌이 배가됐다. `order`를 2 / 5 / 9로 분산해 사이에 무채색 작품이 오게 했다
    (트리맵은 DOM 순서대로 패킹하므로 order가 곧 인접성이다)
  - 모바일은 별도 규칙(테두리 1px·여백 0의 전면 모자이크)이라 영향 없음
- [x] **6-4. 크롬 통일**
  - 모션 토큰 3단계(`--dur-fast/base/slow`) + 이징 1종(`--ease`). 산재하던 **16가지 지속시간 47곳**을 스냅
  - 스페이싱 토큰 4단계(`--space-1~4`)
  - 컨트롤 형태 규칙 명문화: 이동=밑줄 / 필터·태그=필+점 / 모드전환=사각 세그먼트 / 외부링크=사각+↗
  - 검색창을 박스 → **밑줄**로. 사이트에서 유일하게 무거운 테두리였고, 검색은 '이동' 계열이다
  - 리스트 호버 통일: events의 배경+들여쓰기(레이아웃이 흔들림) → 색 변화 하나로
- [x] **6-5. 언어 시스템 — EN 기본 + 전역 KO 토글**
  - 이전엔 방식이 4가지 공존: 2단 병기(Works·작품상세) / 페이지 스코프 토글(Sound·모바일 Works) /
    별도 라우트(CV·About) / 그냥 둘 다 표시(Contact·이벤트)
  - **3층 규칙으로 통일**
    · 크롬(메뉴·라벨·버튼·태그·날짜·빈 상태) = 영문 고정, 전환 없음
    · 서술 콘텐츠 = EN 기본 + 전역 토글 (`html[data-lang]` + `[data-language-content]`)
    · 고유명사 = 원문 유지
  - 토글은 `SiteMenu`에 하나. `localStorage`로 유지되고 **페인트 전에 복원**해 깜빡임이 없다
  - CV·About은 별도 라우트(`/cv` ↔ `/cv/ko`)라 토글이 **이동으로 처리**한다.
    해당 URL로 직접 들어오면 그 URL이 곧 선택으로 기록된다
  - **2단 병기 해체 → 본문이 제 폭(62ch)을 되찾음.** `.textcols`(46.5% 2단 float),
    `.intro-copy`, `.process-note-cols`, `.media-note-cols` 전부 단일 열로
  - 제거: 페이지별 토글 UI 3벌, 그 스크립트 3벌, 스코프 CSS 3벌, CV 자체 EN/KO 스위치
- [x] **6-6. 히어로 워드마크 · 404 · 마이크로카피**
  - 데스크톱 히어로에 **워드마크 상시 노출**. 이전엔 커서를 구체에 넣어야 이름이 나타나
    처음 온 사람이 누구 사이트인지 모른 채 스크롤을 도박해야 했다
  - **404 신설** — 흩어진 입자(구체가 형태를 잃은 상태). 없는 주소가 HTTP 200을 반환하던 문제도 해소
  - 푸터: `COPYRIGHT © … All rights reserved.` → `© 2014–2026 HWANYUN`
    (COPYRIGHT와 ©는 동어반복, All rights reserved는 1990년대 법률 부적)
  - eyebrow에서 `HWANYUN /` 접두 제거 — 이름은 좌상단에 상시 있으므로 반복은 소음
  - 빈 상태 3종 3언어 → 영문 하나로 (크롬은 영문 고정 규칙)
  - `Sharing materials` → `Related by material` (동명사가 주어처럼 읽혀 어색했다)
  - `--text-4` #aaa → **#767676**. 흰 배경 2.3:1로 11px eyebrow에 쓰기엔 읽을 수 없었다 (→ 4.5:1)

## Phase 7 — 히어로 워드마크의 커서 반발 (2026-08-15)

- [x] **7-1. 이름이 모인 뒤 커서가 글자를 밀어낸다** — Phase 3 "커서=센서"의 히어로 확장.
  이전까지 커서는 구체↔이름 on/off 스위치였고, 모인 뒤에는 아무 일도 없었다
  - 반경 `REPEL_RADIUS` 128px, 세기 `REPEL_FORCE` 2.6 (접촉점 최대 밀림 = FORCE/스프링계수 ≈ 104px)
  - **반발은 감쇠(`* .8`) 안에서 더해야 한다.** 밖에서 더하면 정상상태 속도가
    `4·spring + 5·push`가 되어 매 프레임 누적되고, 글자가 원반으로 터진다
  - 기존 스프링이 그대로 살아 있어 커서를 치우면 글자가 스스로 되모인다
  - 히스테리시스: 들어갈 때는 구체 안, 흩어질 때는 **워드마크 사각형** 기준.
    워드마크가 구체보다 넓어서, 같은 기준을 쓰면 글자를 만지려는 순간 구체로 되돌아간다
  - `prefers-reduced-motion`·터치·모바일에서는 동작하지 않는다
- [x] **7-1b. 모바일에서도 밀 수 있게** — 터치엔 hover가 없어 처음엔 데스크톱 전용으로 막아 뒀다.
  대신 **손가락이 닿아 있는 동안**을 커서와 같은 입력으로 삼는다
  - 제스처가 갈려서 서로 방해하지 않는다: **탭(이동 12px 미만) = 구체↔이름 전환, 드래그 = 밀기**
  - 손을 떼면 밀림이 풀리고 스프링이 글자를 되모은다
  - **반경·세기를 워드마크 높이에 비례**시켰다 (데스크톱 `×1.38`/`×.028`, 모바일 `×2.1`/`×.08`). 고정값(128 / 2.6)은
    글자 높이 93px인 데스크톱 기준이라, 40px인 모바일에 그대로 쓰면 이름 전체가 날아간다
- [x] **7-2. 워드마크 폭 측정 버그** — 라이브 콘솔의 `IndexSizeError: getImageData ... width is 0`의 정체
  - 폭 0에서 샘플링하면 예외로 죽고, 이후 `window.resize`가 오지 않으면 워드마크가 낡은 폭으로 굳는다.
    실제로 1280px 화면에 312px짜리 이름이 찍히고 있었다(= 구체도 함께 작게 그려짐)
  - 폭 0 가드 + `ResizeObserver`. 단, `resize()`는 입자를 중앙으로 리셋하므로
    **크기가 같으면 재구축을 건너뛴다** — 아니면 반복 호출로 이름이 영영 모이지 못한다
  - 폭이 제자리를 찾으니 구체가 의도대로 커졌다. 화면을 너무 채워 `globeRadius` 계수를 `.49 → .38`로
- [x] **7-3. 언어 토글 — 문맥에서 색을 상속** (6-5의 후속)
  - 원인은 색이 아니라 **키잉**이었다. `<body data-page={current || "home"}>`에서
    `current`는 "메뉴 하이라이트"를 뜻하는데, 없으면 홈으로 간주해 다크 팔레트를 씌웠다.
    `current`를 넘기지 않는 작품 상세·404가 흰 배경에 `#ececea`(대비 1.1:1)로 찍혔다
  - 토글이 자기 팔레트를 갖지 않게 했다 — `--chrome-fg` 상속 + 비활성은 `currentColor 55%`.
    어두운 첫 화면을 가진 페이지만 `<Base chrome="dark">`로 **명시**하고, 잊으면 밝은 쪽으로 폴백한다
  - `data-page` 속성과 페이지별 규칙 5줄이 통째로 사라졌다(유일한 용도였다).
    대비: 활성 18.9:1, 비활성 ≈ 4.8:1 (이전 2.8:1)
- [x] **7-4. 모바일 언어 토글을 헤더로** — 이전엔 햄버거를 열어야만 나왔다.
  크롬이 영문 고정이라, 패널 안에만 두면 **한국어가 있다는 사실 자체가 드러나지 않는다**
  - 모바일 헤더를 3열로(이름 | EN/KO | ☰). 같은 그리드 안이라 정렬 매직 넘버가 없다.
    햄버거는 그대로 우측 끝
  - 색은 7-3의 `currentColor` 상속을 그대로 탄다 — 히어로 위에서는 밝게, 흰 페이지에서는 어둡게
  - 활성 밑줄을 `border-bottom` → `text-decoration`(offset 4px)으로. 터치 타깃 패딩(13px)을 주면
    border 밑줄은 글자에서 떨어져 뜬다. 데스크톱 코너도 같은 방식으로 통일
  - 패널 안 중복 토글 제거
- [x] **7-5. Works 필드 뷰 삭제** (5-1 철회)
  - 회색 점 군집 12개에 축·라벨·범례가 없어 무엇을 뜻하는지 읽을 수 없었다.
    만든 사람이 못 읽으면 큐레이터는 더 못 읽는다
  - **고쳐 살리지 않고 지운 이유**: 담고 있던 정보가 이미 더 정확한 형태로 두 군데에 있다 —
    연도는 목록의 YEAR 열, 재료 수는 필터 알약의 숫자(`Copper 4`). 축을 붙여 읽히게 만들어도
    새로 알려주는 것이 없다. 원칙 1("답이 없으면 넣지 않는다")의 적용
  - 모드 전환 컨트롤·캔버스 스크립트·포인터/리사이즈 핸들러까지 214줄 제거.
    Phase 4의 재료 필터 회로 뷰는 그대로 남는다(그쪽은 목록 위에서 동작한다)
- [x] **7-6. 스폰서 로고 원색 복원** (6-1 재검토) — 기관 로고는 남의 아이덴티티다.
  무채색 크롬 규칙의 예외로 두는 것이 맞다 (작가 판단)

## 페이즈 공통 완료 기준

1. `astro build` 통과
2. 데스크톱 + 모바일(700px) 스크린샷 검증
3. `prefers-reduced-motion: reduce`에서 정적 아카이브로 완전 폴백
4. 새 hex 색상·font-size 추가 금지 — Phase 1 토큰만 사용
5. 커밋 전 임시 테스트 데이터(`zztmp-*`) 제거 확인

## 참고

- 로컬 dev: `node node_modules/astro/astro.js dev --port 4330`
  (node는 `~/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node`)
- 패키지 매니저: pnpm (`pnpm-lock.yaml` 커밋 여부 결정 필요 — 기존 `package-lock.json`과 중복)
- 벤치마크 근거: Zimoun(움직이는 썸네일·Next Exhibitions 단일 리스트), Ikeda(메타데이터 규율),
  Bernier(Until 날짜 표기), Nicolai(연도 아카이브), Anadol(반면교사 — 스펙터클 과잉)
- 배포 검증 절차는 `PROJECT_CONTEXT.md` 기준
